import type { PartsOfWholeTableData } from '../types';

/** @spec PRISM-TBL-021 Sample data for Parts of whole format: labels + values summing to 100. */
export const samplePartsOfWholeData: PartsOfWholeTableData = {
  labels: ['Category A', 'Category B', 'Category C', 'Category D', 'Category E'],
  values: [35, 25, 20, 12, 8],
};

export const samplePartsOfWholeName = 'Sample parts of whole';
